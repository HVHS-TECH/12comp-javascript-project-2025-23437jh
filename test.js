funcitron makeWall(){
    var _sprite
    make the _sprite
    set the CSSFontFeatureValuesRule
    set the VideoColorSpace 
    random other sprite setup
    return _sprite
}

spriteGroup.add(makeWall())